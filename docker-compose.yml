version: '3.8'

services:
  webchat:
    build: ./webchat
    container_name: webchat-app
    ports:
      - "3000:3000"
    env_file:
      - .env.secret
    restart: unless-stopped
    networks:
      - lms-network

networks:
  lms-network:
    name: lms-network
    attachable: true
